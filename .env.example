# API Configuration for The Last Vote
# ========================================
# This file shows all available environment variables.
# Copy this file to .env.local and configure as needed.
# ========================================

# API Mode Selection
# -----------------
# Controls which mode the API client uses:
# - Leave empty to use FALLBACK mode (offline, instant Thai responses)
# - Set to 'mock' for MOCK mode (simulated delays for testing)
# - Set VITE_API_URL to enable API mode (real backend with AI)

# Enable MOCK mode (simulated delays for testing UI states)
VITE_API_MODE=mock

# Enable API mode (real backend with AI-generated responses)
# Uncomment and set your backend URL when ready:
# VITE_API_URL=https://your-backend.vercel.app/api/chat

# Optional Configuration
# ---------------------
# Override request timeout in milliseconds (default: 10000)
# VITE_API_TIMEOUT=10000

# Override max retry attempts (default: 3)
# VITE_API_MAX_RETRIES=3

# Override initial retry delay in milliseconds (default: 1000)
# VITE_API_RETRY_DELAY=1000
